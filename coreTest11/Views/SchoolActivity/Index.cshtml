@model IEnumerable<coreTest11.Models.SchoolActivity>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TripName)
                ClassRoom
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @*<a asp-controller="Developments" asp-route-id="@item.DID">@Html.DisplayFor(modelItem => item.DName)</a>*@
                    @item.TripName
                    @item.ActivityDT
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ActivityID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.ActivityID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.ActivityID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
